# Sidiora Launchpad AMM — Integration Kit

Auto-generated by `scripts/generate-integration-kit.js`.

## Structure

```
integration-kit/
  env/                          # Environment variable templates
    .env.template               # Generic (no prefix)
    .env.local.nextjs           # Next.js (NEXT_PUBLIC_)
    .env.vite                   # Vite (VITE_)
    .env.cra                    # Create React App (REACT_APP_)
    .env.nuxt                   # Nuxt 3 (NUXT_PUBLIC_)
    .env.sveltekit              # SvelteKit (PUBLIC_)
    .env.remix                  # Remix (no prefix)
    .env.node                   # Node.js backend (no prefix)
    .env.python                 # Python backend (no prefix)
  abi/                          # Contract ABIs
    {Name}.json                 # Raw ABI array
    {Name}.js                   # CommonJS export
    {Name}.ts                   # TypeScript const assertion (viem/wagmi)
    index.js                    # Barrel export (CJS)
    index.ts                    # Barrel export (ESM)
    index.json                  # Name → file map
```

## Network

- **Chain**: Paxeer Network (ID: 125)
- **RPC**: https://public-mainnet.rpcpaxeer.online/app
- **Explorer**: https://paxscan.paxeer.app
- **Native**: PAX

## Available ABIs

| ABI | Use Case |
|-----|----------|
| `ProtocolConfigABI` | Global protocol parameters & feature flags |
| `TreasuryABI` | Protocol fee collection & disbursement |
| `TimelockABI` | Time-delayed governance actions |
| `GovernanceModuleABI` | On-chain governance (proposals, voting) |
| `EventEmitterABI` | Centralized event emission for indexers |
| `PoolRegistryABI` | Pool discovery & metadata lookup |
| `FeeAccumulatorABI` | Per-pool fee tracking & distribution |
| `SidioraFactoryABI` | Pool creation & deployment |
| `SidioraPoolABI` | Per-pool AMM logic (deployed via beacon proxy) |
| `PoolBeaconABI` | Upgradeable pool implementation pointer |
| `SidioraERC20ABI` | Pool LP token (per-pool) |
| `SidioraNFTABI` | Position NFT for liquidity providers |
| `OpticalRegistryABI` | Optical module registration & lookup |
| `AntiSnipeOpticalABI` | Anti-bot snipe protection at launch |
| `MaxWalletOpticalABI` | Per-wallet position cap |
| `TaxOpticalABI` | Buy/sell tax routing |
| `CooldownOpticalABI` | Trade cooldown enforcement |
| `BuybackBurnOpticalABI` | Automated buyback & burn from fees |
| `RouterABI` | User entry point — swap, add/remove liquidity |
| `QuoterABI` | Off-chain quote simulation (swap pricing) |
| `FeesRouterABI` | Fee collection & routing entry point |
| `MetaAGRouterABI` | Multi-protocol aggregated swap routing |
| `MetaAGQuoterABI` | Multi-protocol aggregated quoting |
| `PECORVaultABI` | PECOR managed vault (capital allocation) |
| `PECORABI` | PECOR engine (predictive execution & routing) |
| `PECOROrdersABI` | PECOR conditional/limit order management |
| `PriceOracleABI` | Aggregated price oracle |
| `OracleHubABI` | Oracle adapter registry & routing |
| `TransactionTrackerABI` | On-chain transaction analytics |
| `VaultAdapterABI` | PECOR vault → external protocol bridge |
| `SidioraAdapterABI` | Meta-AG → Sidiora pool adapter |
| `PriceOracleAdapterABI` | External price feed adapter |
| `SidioraFeedAdapterABI` | Sidiora pool price feed adapter |
| `IRouterABI` | Router typed interface |
| `IQuoterABI` | Quoter typed interface |
| `IFeesRouterABI` | FeesRouter typed interface |
| `ISidioraPoolABI` | Pool typed interface |
| `ISidioraFactoryABI` | Factory typed interface |
| `ISidioraNFTABI` | Position NFT typed interface |
| `IProtocolConfigABI` | ProtocolConfig typed interface |
| `ITreasuryABI` | Treasury typed interface |
| `IFeeAccumulatorABI` | FeeAccumulator typed interface |
| `IPoolRegistryABI` | PoolRegistry typed interface |
| `IEventEmitterABI` | EventEmitter typed interface |
| `IMetaAGRouterABI` | Meta-AG router typed interface |
| `IMetaAGQuoterABI` | Meta-AG quoter typed interface |
| `IPECORVaultABI` | PECORVault typed interface |
| `IPECORABI` | PECOR engine typed interface |
| `IPECOROrdersABI` | PECOROrders typed interface |
| `IPriceOracleABI` | PriceOracle typed interface |
| `IOracleHubABI` | OracleHub typed interface |
| `ITransactionTrackerABI` | TransactionTracker typed interface |
| `IProtocolAdapterABI` | Protocol adapter typed interface |
| `IDataFeedAdapterABI` | Data feed adapter typed interface |

## Quick Start (Next.js + viem)

```ts
import { createPublicClient, http } from "viem";
import { RouterABI } from "./abi";

const paxeer = {
  id: 125,
  name: "Paxeer Network",
  nativeCurrency: { name: "PAX", symbol: "PAX", decimals: 18 },
  rpcUrls: { default: { http: ["https://public-mainnet.rpcpaxeer.online/app"] } },
} as const;

const client = createPublicClient({ chain: paxeer, transport: http() });

const amountOut = await client.readContract({
  address: process.env.NEXT_PUBLIC_ROUTER_ADDRESS as `0x${string}`,
  abi: RouterABI,
  functionName: "getAmountOut",
  args: [amountIn, path],
});
```

## Quick Start (Node.js + ethers)

```js
const { ethers } = require("ethers");
const { RouterABI } = require("./abi");

const provider = new ethers.JsonRpcProvider("https://public-mainnet.rpcpaxeer.online/app");
const router = new ethers.Contract(process.env.ROUTER_ADDRESS, RouterABI, provider);

const reserves = await router.getReserves(tokenA, tokenB);
console.log(reserves);
```

## Deployment File Format

Place at `deployments/sidiora.json`:

```json
{
  "chainId": 125,
  "contracts": {
    "ProtocolConfig":   "0x...",
    "Treasury":         "0x...",
    "Timelock":         "0x...",
    "GovernanceModule": "0x...",
    "EventEmitter":     "0x...",
    "PoolRegistry":     "0x...",
    "FeeAccumulator":   "0x...",
    "SidioraFactory":   "0x...",
    "PoolBeacon":       "0x...",
    "SidioraNFT":       "0x...",
    "OpticalRegistry":  "0x...",
    "Router":           "0x...",
    "Quoter":           "0x...",
    "FeesRouter":       "0x...",
    "MetaAGRouter":     "0x...",
    "MetaAGQuoter":     "0x...",
    "PECORVault":       "0x...",
    "PECOR":            "0x...",
    "PECOROrders":      "0x...",
    "PriceOracle":      "0x...",
    "OracleHub":        "0x..."
  },
  "tokens": [
    { "symbol": "WPAX", "address": "0x..." },
    { "symbol": "USDC", "address": "0x..." }
  ]
}
```

> **Note**: `SidioraPool` and `SidioraERC20` are deployed per-pool via the beacon
> proxy pattern, so they don't get a fixed address in env files. Look up live pool
> addresses via `SidioraFactory.getPool(tokenA, tokenB)` or `PoolRegistry`.
